name = "moonbase"

version = "0.1.0"

description = "Zero-dependency, cross-backend low-level infrastructure for MoonBit: block allocators and fixed-capacity containers."

readme = "README.md"

repository = "https://github.com/QYF795/moonbase"

license = "Apache-2.0"

keywords = [ "allocator", "memory", "ring-buffer", "bitvec", "data-structures" ]

source = "src"