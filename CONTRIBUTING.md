# 贡献协议（AI 提交契约）

本仓库的代码主要由 AI 辅助生成，人类负责方向裁决、代码审查与最终核验。
以下契约对**所有**提交（无论作者）生效。

## PR 规则

1. **一条 PR 只做一件事**：新增模块、修复、重构分开提交
2. **单条 PR ≤ 300 行**（不含测试与自动生成文件）
3. **必须带测试**：
   - 每个公开 API 至少有 1 条单元测试
   - 每个模块至少有 1 条基于确定性伪随机序列的属性测试（不变量校验）
4. **CI 全绿**：`moon check` 零警告 + 三目标测试全过 + 零 FFI 检查过
5. **复杂度标注**：每个公开 API 的文档注释必须标注时间复杂度；新分配器必须标注最坏情况碎片率

## 审查清单（人类审查者逐项核验）

- [ ] 不变量论证：分配器测试是否真正校验了「不重叠、对齐、可复用」？
- [ ] 边界：0 容量、负参数、越界、OOM 路径是否都有测试覆盖？
- [ ] 契约：`Option`=资源耗尽、`abort`=契约违反 是否被正确区分？
- [ ] 零依赖红线：`grep -rEn 'extern "C"|extern "wasm"' src` 无输出
- [ ] 文档：README/design.md 是否需要同步更新？
- [ ] 基准：涉及性能的改动是否在 docs/benchmarks.md 记录了对比数据？

## 目录约定

- `src/mem/`：分配器（每个实现一个文件，trait 在 `allocator.mbt`）
- `src/collections/`：定容容器（每个容器一个文件）
- 测试与源码同文件（`test "..." {}` 块），保持模块自包含
- 提交信息格式：`feat(mem): add buddy allocator` / `fix(collections): ...`
